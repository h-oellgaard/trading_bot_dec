[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
markers =
    integration: marks tests that hit external APIs (e.g. Firi) - run with -m integration
# Run unit tests only: pytest tests/unit
# Run integration tests only: pytest tests/integration or pytest -m integration
